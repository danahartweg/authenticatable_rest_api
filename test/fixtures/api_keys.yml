dan_session:
  user: dan
  access_token: <%= SecureRandom.hex %>
  scope: 'api'
  expires_at: <%= 4.hours.from_now %>

susie_api:
  user: susie
  access_token: <%= SecureRandom.hex %>
  scope: 'api'
  expires_at: <%= 30.days.from_now %>
